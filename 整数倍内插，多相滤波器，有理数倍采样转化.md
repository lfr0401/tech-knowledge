# 信号整数倍内插，抽取
采样率变换是数字信号处理、软件无线电、通信基带处理中的基础核心环节。整数倍抽取与整数倍内插是实现数字域采样率调整的两种基础操作，分别对应信号降采样与升采样。

## 1.抽取
### 1.1 时域定义
设抽取因子为正整数 $D$，抽取后信号每隔 $D$ 点取一个值：
$$x_d[n] = x[Dn]$$
抽取后采样周期、采样频率：
$$T_d = D T_s,\quad f_{sd} = \frac{f_s}{D}$$

### 1.2 频谱原理与核心公式
对离散信号做 $D$ 倍抽取，本质是降低信号采样率，会改变信号频谱，极易引发频谱混叠。
抽取频谱核心公式（最简结论）：
$$X_d(e^{j\omega}) = \frac{1}{D}\sum_{m=0}^{D-1} X\left(e^{j\frac{\omega-2\pi m}{D}}\right)$$
原理说明：抽取后的频谱是原频谱经拉伸、移位、叠加后取平均的结果。公式体现两个关键特性：一是频谱幅度整体衰减为原来的 
$$1/D$$
；二是原频谱被拉伸 $D$ 倍，多个频谱周期会重叠，这就是抽取产生混叠的根本原因。

### 1.3 无混叠条件
抽取会拉伸频谱、造成频谱重叠（混叠），因此必须限制信号带宽，保证拉伸后的频谱不重叠。
原理推导（极简版）
原始离散信号的数字频率天然受奈奎斯特带宽限制：
$$|\Omega| < \pi$$

$D$倍抽取的物理效果：时域采样点变稀疏，频谱会被整体横向拉伸 $D$ 倍，满足映射关系：
$$\omega_{\text{new}} = D\cdot \Omega$$

抽取后的新信号依然要满足数字奈奎斯特约束：
$$|\omega_{\text{new}}| < \pi$$
因此原信号最大频率必须满足：
$$D\cdot \Omega_{\text{max}} < \pi$$

因此得到数字域无混叠条件：
$$\Omega_{\text{max}} < \frac{\pi}{D}$$
代入数字角频率与模拟频率关系
$$\Omega=\dfrac{2\pi f}{f_s}$$
代入化简：
$$\frac{2\pi f_{\text{max}}}{f_s} < \frac{\pi}{D} \implies f_{\text{max}} < \frac{f_s}{2D}$$
核心结论：抽取倍数越大，允许的信号带宽越小，所以大倍率抽取前必须低通滤波限带。

### 1.4 工程实现（抗混叠抽取）
先低通滤波、后抽取：
$$x_f[n] = x[n] * h_d[n],\quad x_d[n] = x_f[Dn]$$
抗混叠滤波器截止频率：
$$\omega_c = \frac{\pi}{D}$$

## 2.内插
内插是在原始采样点之间补零，提高采样率，时域变密，对应频谱被压缩，和抽取的频谱拉伸完全相反。
根据零填充定义，只有 $n=Ik$ 处有值，其余为0：
$$X_u(e^{j\omega}) = \sum_{k=-\infty}^{\infty} x[k] e^{-j\omega I k} = X(e^{jI\omega})$$
内插频谱核心公式：
$$X_u(e^{j\omega}) = X(e^{jI\omega})$$
物理原理（重点）
1. 抽取：时域变稀疏 → 频谱拉伸
2. 内插：时域变密集 → 频谱压缩 $I$ 倍

频谱被压缩后，原本 
$$[0,2\pi]$$ 
内的单周期频谱会反复折叠，因此区间内会出现 $I$ 个重复的镜像频谱。
镜像频率怎么算，镜像中心位置：
$$\(\omega'_k = \pm\frac{2\pi k}{L},\quad k=1,2,\dots,L-1\)$$

# 多相滤波器原理（传统架构缺陷 + 多相分解核心思想）
## 传统架构存在的问题
传统采样率变换架构会产生大量无效计算，浪费 DSP 硬件与软件算力：
- **抽取**：先完整滤波，再做下采样。滤波器依然计算后续会被丢弃的采样点。
- **内插**：先插零，再完整滤波。大量零值参与乘累加，出现很多无效的乘 0 运算。

多相滤波器核心目标：消除上述无效运算，把滤波和采样率变换合并，降低计算量。

## 多相分解定义
设采样率变换因子为 $L$（抽取、内插共用该因子）。
将原型滤波器冲激响应 $h[n]$，对系数下标对 $L$ 取模，拆分为 $L$ 路子滤波器（多相分支）。
- $k$：多相分支编号，
- $$k=0,1,\dots,L-1$$
- 一共 $L$ 路
- $m$：单路子滤波器内部的系数索引

**物理含义**
把一条长原型滤波器切分为 $L$ 条短子滤波器；每一路只取原滤波器下标间隔为 $L$ 的系数，直接避开冗余计算。

## 多相分解 Z‑域核心公式
原型滤波器 Z 变换：
$$H(z)=\sum_{n=0}^{N-1} h[n]\, z^{-n}$$

多相分解表达式：
$$H(z)=\sum_{k=0}^{L-1} z^{-k}\, H_k(z^{L})$$

第 $k$ 路多相子滤波器：
$$H_k(z)=\sum_{m} h\left[mL+k\right] z^{-m}$$

# 有理数倍采样率转换
分数倍采样率转换倍率：
$$R=\dfrac{I}{D}$$

流程：先 $I$ 倍多相内插，再 $D$ 倍多相抽取；可以共用一组多相滤波器，无需分别做两次完整滤波。
